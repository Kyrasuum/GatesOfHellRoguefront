package data

import (
// "fmt"

// "roguefront/pkg/app"
)

func (inf Infantry) Display(editable bool, exit func()) {
	// width := float32(app.CurApp.GetWidth())
	// height := float32(app.CurApp.GetHeight())
	//
	// gui.Panel(rl.NewRectangle((width-300)/2, (height-200)/2, 300, 200), fmt.Sprintf("%s", inf.Name))
	// cnbtn := gui.Button(rl.NewRectangle((width-300)/2+278, (height-200)/2+2, 20, 20), "X")
	//
	//	if cnbtn {
	//		exit()
	//	}
}

func (sqd Squad) Display(editable bool, exit func()) {
	// width := float32(app.CurApp.GetWidth())
	// height := float32(app.CurApp.GetHeight())
	//
	// gui.Panel(rl.NewRectangle((width-300)/2, (height-200)/2, 300, 200), fmt.Sprintf("%s", sqd.Name))
	// cnbtn := gui.Button(rl.NewRectangle((width-300)/2+278, (height-200)/2+2, 20, 20), "X")
	//
	//	if cnbtn {
	//		exit()
	//	}
}

func (veh Vehicle) Display(editable bool, exit func()) {
	// width := float32(app.CurApp.GetWidth())
	// height := float32(app.CurApp.GetHeight())
	//
	// gui.Panel(rl.NewRectangle((width-300)/2, (height-200)/2, 300, 200), fmt.Sprintf("%s", veh.Name))
	// cnbtn := gui.Button(rl.NewRectangle((width-300)/2+278, (height-200)/2+2, 20, 20), "X")
	//
	//	if cnbtn {
	//		exit()
	//	}
}

func (inv Inventory) Display(editable bool, exit func()) {
	// width := float32(app.CurApp.GetWidth())
	// height := float32(app.CurApp.GetHeight())
	//
	// gui.Panel(rl.NewRectangle((width-300)/2, (height-200)/2, 300, 200), fmt.Sprintf("%s", inv.Name))
	// cnbtn := gui.Button(rl.NewRectangle((width-300)/2+278, (height-200)/2+2, 20, 20), "X")
	//
	//	if cnbtn {
	//		exit()
	//	}
}

func (inv Item) Display(editable bool, exit func()) {
	// width := float32(app.CurApp.GetWidth())
	// height := float32(app.CurApp.GetHeight())
	//
	// gui.Panel(rl.NewRectangle((width-300)/2, (height-200)/2, 300, 200), fmt.Sprintf("%s", inv.Name))
	// cnbtn := gui.Button(rl.NewRectangle((width-300)/2+278, (height-200)/2+2, 20, 20), "X")
	//
	//	if cnbtn {
	//		exit()
	//	}
}
